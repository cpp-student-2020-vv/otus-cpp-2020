#pragma once

#include <iostream>
#include <vector>

void task_2001(std::vector<int>& a, std::vector<int>& b, int& out_a, int& out_b);
void task_2001_io_wrapper(std::istream& in = std::cin,
                          std::ostream& out = std::cout);

